// let values = [5,7,8];
// console.log(values);
// values[1]=6;
// console.log(values);

// Practicing multiple data types in an array
// let data = ["hello",3, {tech:'JS'}, function(){return "hello"}]

// console.log(data);

// let gotData = data[3]();
// console.log(gotData);

// let objectTech = data[2].tech;
// console.log(objectTech);

// Trying methods in array object
let newData = [5,798,45,432,54]
//console.log(newData.push(10)); // puts new value to an array (prints the new length of the array)
//console.log(newData.pop()); // removes the last inserted value in an array (very basic knowledge) (prints the value that is removed)
//console.log(newData.shift()); // removes the first data inserted value in an array (prints the value that was deleted)
//console.log(newData.unshift()); // adding an element from the start of the array  (prints the new length of the array)\

// SYNTAX: splice(index Number, number of elements to be removed, elements that will replace the elements removed (can be array))
// console.log(newData.splice(3));// removes everything starting from the INDEX number given. (prints the values removed from the array)
// console.log(newData.splice(2,2)); // the 2nd parameter is an indicator how many elemets are you going to remove from the array. (prints the values removed from the array)
// console.log(newData.splice(1,3,12,13,14));  // 3rd parameter adds new values to the array. (prints the values removed from the array)

console.log(newData);